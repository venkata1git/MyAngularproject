import { AppendTextPipe } from './append-text-pipe';

describe('AppendTextPipe', () => {
  it('create an instance', () => {
    const pipe = new AppendTextPipe();
    expect(pipe).toBeTruthy();
  });
});
